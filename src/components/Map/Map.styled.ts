import styled from 'styled-components';

export const MapWrapper = styled.div`
    background: green;
    width: 100vw;
    height: 100vh;
    font-size: 100px;
    overflow: hidden;
    position: relative;
    contain: strict;
`;
