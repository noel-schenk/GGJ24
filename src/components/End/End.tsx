import React, { FC } from 'react';
import { EndWrapper } from './End.styled';

interface EndProps {}

const End: FC<EndProps> = () => (
 <EndWrapper>
    End Component
 </EndWrapper>
);

export default End;
