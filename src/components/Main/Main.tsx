import React, { FC } from 'react';
import { MainWrapper } from './Main.styled';

interface MainProps {}

const Main: FC<MainProps> = () => (
 <MainWrapper>
    Main Component
 </MainWrapper>
);

export default Main;
