import React, { FC } from 'react';
import { ChatWrapper } from './Chat.styled';

interface ChatProps {}

const Chat: FC<ChatProps> = () => (
 <ChatWrapper>
    Chat Component
 </ChatWrapper>
);

export default Chat;
